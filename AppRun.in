#!/bin/bash
HERE="$(dirname "$(readlink -f "${0}")")"
export ABUSE_PATH="${HERE}/usr/@CMAKE_INSTALL_DATADIR@/abuse"
export LD_LIBRARY_PATH="${HERE}/usr/@CMAKE_INSTALL_LIBDIR@:${LD_LIBRARY_PATH}"
exec "${HERE}/usr/bin/abuse" "$@"
